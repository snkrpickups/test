name: Deploy landing page to GitHub Pages

on:
  push:
    branches:
      - main
      - master
      - claude/elegant-fermat-hsFXO
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Stage site files
        run: |
          mkdir -p _site/assets
          cp index.html _site/index.html
          cp thankyou.html _site/thankyou.html
          cp colors_and_type.css _site/colors_and_type.css
          cp assets/fox-mark.png _site/assets/fox-mark.png
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
